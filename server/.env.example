# Copy to .env and fill in values locally.
# NEVER commit real secrets. Rotate any key that was ever committed.

GEMINI_API_KEY=
PORT=3001

# Optional security-related overrides
# MAX_FILE_SIZE_MB=5
# MAX_PROMPT_LENGTH=4000
# CORS_ORIGIN=http://localhost:5173
